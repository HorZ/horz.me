<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel="stylesheet' href='/bootstrap/css/bootstrap.min.css" />
  </head>
  <body>
  	<header>
  		<nav>
  			<a href="#">a</a>
  			<a href="#">b</a>
  			<a href="#">c</a>
  		</nav>
  	</header>
    <input type="file" id="take-picture" accept="image/*">
    <input type="button" text="show" id="show-location" onclick="navigator.geolocation.getCurrentPosition(showLocation);">
    <script>
      function showLocation(position){
        var latitude = position.coords.latitude;
        var longitude = position.coords.longitude;
        alert("[" + latitude + "]-[" + longitude + "]")
      }
    </script>
    <h1>{{ title }}</h1>
    <p>Welcome to {{ zone }}</p>
    <div>{{anyinfo}}</div>
    <footer id="footer">
    	<div class="container">
    		<p class="text-muted credit">
	    		<a href="http://www.miitbeian.gov.cn/">沪ICP备13037527号</a>
	    	</p>
    	</div>
    </footer>
  </body>
</html>